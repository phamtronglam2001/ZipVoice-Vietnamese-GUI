# ZipVoice Vietnamese TTS — portable offline GUI
# k2 is optional for inference; omitted for simpler CPU/GPU setup.

torch>=2.1.0,<=2.6.0
torchaudio>=2.1.0,<=2.6.0
lhotse
tensorboard
vocos
safetensors
gradio>=4.44.0
huggingface_hub

# Text normalization (GUI: chọn 1 trong các backend)
sea-g2p>=0.7.5
pydub
librosa
matplotlib
numpy>=1.24.0,<=1.26.4
scipy
soundfile
tqdm>=4.65.0
ema_pytorch>=0.5.0
x_transformers>=1.31.14
torchdiffeq
cn2an
inflect
unidecode
pypinyin
jieba

# Espeak tokenizer — install via setup.ps1 with k2-fsa wheel index
# (listed separately because it needs --find-links on some platforms)
